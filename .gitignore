# ─────────────────────────────────────────────
# Track ONLY the deployable build/ folder.
# Everything else (docs, source PSDs, assets,
# tooling, _unused dead files) stays local.
# ─────────────────────────────────────────────
/*
!/.gitignore
!/build/

# OS junk inside build/
**/.DS_Store
**/desktop.ini
