# This repo IS the deployable site (the build/ folder).
# All docs, source assets, and non-essentials live in the
# PARENT directory, outside this repo entirely.

# OS junk
**/.DS_Store
**/desktop.ini
